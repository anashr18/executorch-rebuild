# Replace the key ID below with yours from the error (example shown)
KEY=3B4FE6ACC0B21F32

# Download directly from Ubuntu keyserver over HTTPS instead of keyserver port
curl -fsSL "https://keyserver.ubuntu.com/pks/lookup?op=get&search=0x$KEY" | \
gpg --dearmor | sudo tee /etc/apt/trusted.gpg.d/$KEY.gpg > /dev/null

sudo apt update

